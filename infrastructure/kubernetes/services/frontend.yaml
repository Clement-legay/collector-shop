apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: collector
  labels:
    app: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/clement-legay/collector-shop/frontend:sha-aabc42116c7d6ec7cf6e257e3f7a8f42ee6a1657
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          env:
            - name: API_BASE_URL
              value: "http://collector.local"

---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: collector
spec:
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: frontend
